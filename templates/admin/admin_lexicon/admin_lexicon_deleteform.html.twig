<form method="post" action="{{ path('admin.lexicon.delete', {'id': mot.id}) }}" style="display: inline-block;" class="no-padding">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mot.id) }}">
    <button class="waves-effect waves-light btn red darken-4 tooltipped" data-position="top" data-tooltip="Supprimer"><i class="material-icons white-text">delete</i></button>
</form>
